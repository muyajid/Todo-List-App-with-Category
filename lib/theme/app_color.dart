import 'package:flutter/material.dart';

class AppColor {
  // --- Primary Color ---

  // Button utama, Add Todo, Save, Login
  static const primaryblue = Color.fromARGB(255, 59, 130, 246);
  // Header, Navbar, dll...
  static const primarydark = Color.fromARGB(255, 30, 58, 138);

  // --- Seconday Color ---

  // Success Notif
  static const secondarygreen = Color.fromARGB(255, 16, 185, 129);
  // Error Notif
  static const secondaryred = Color.fromARGB(255, 239, 68, 68);

  // --- Neutral Color ---

  // Background Utama
  static const neutrallight = Color.fromARGB(255, 249, 250, 251);
  // Teks sekunder -> Deskripsi, dll...
  static const neutralgraymedium = Color.fromARGB(255, 156, 163, 175);
  // Teks primer -> Title, Teks utama
  static const neutralgraydark = Color.fromARGB(255, 55, 65, 81);
}
